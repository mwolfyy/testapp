# streamify

## Project setup
```sh
# setup the database
python3 setup_db

# install dependencies
pip3 install flask
pip3 install flask_cors

# start the flask api
python3 app.py

# install any module dependacies needed
npm install

# start the vue frontend server
npm run serve
``` 
